name: CICD-SEC-6 Shared secrets abuse

on: pull_request

jobs:
    test:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout code
              uses: actions/checkout@v6
              
            - name: Test application
              run: echo "Testing the application..."
